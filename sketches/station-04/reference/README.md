# Reference photos

Drop photos of the building here, named `ref-01.jpg`, `ref-02.jpg`, …
6–10 photos covering different lighting / weather conditions is a good
target. `.jpg`, `.jpeg`, `.png` all fine.

Then list them under `"reference_photos"` in `../metadata.json`.
